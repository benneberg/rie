# rie.yaml
governance:
  strict_mode: true
  thresholds:
    min_fitness_score: 80
    max_stability_drop: 0.05
    allow_layer_bypass: false
  
  notifications:
    github_pr_comment: true
    slack_webhook: ${{ secrets.SLACK_WEBHOOK }}
