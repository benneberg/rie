# rie
Repository Intelligence Engine (RIE) is a structured codebase analysis system that extracts architectural, semantic, and operational intelligence from any source repository (Git-based or file-system based), and transforms it into:  RIE does not merely document code — it makes repositories machine-explainable.
